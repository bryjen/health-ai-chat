@inherits ShadcnComponentBase
@using WebFrontend.Components.Chat.Models

<div class="w-full flex justify-end text-sm pt-8">
    <div class="w-fit p-2 bg-primary/50 border border border-primary/20 rounded-lg">
        @Message.GetRawContent()
    </div>
</div>

@code
{
    [Parameter]
    public required UserChatMessage Message { get; set; }
}
