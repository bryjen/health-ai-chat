using Microsoft.AspNetCore.Components;
using WebFrontend.Utils;

namespace WebFrontend.Components.UI.Alert;

[ComponentMetadata(
    Description = "Inline alert surface for status and feedback messages.",
    IsEntry = true,
    Group = nameof(Alert))]
public partial class Alert
{
}

