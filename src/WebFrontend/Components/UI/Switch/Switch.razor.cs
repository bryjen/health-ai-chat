using Microsoft.AspNetCore.Components;
using WebFrontend.Utils;

namespace WebFrontend.Components.UI.Switch;

[ComponentMetadata(
    Description = "Toggle switch component for boolean values.",
    IsEntry = true,
    Group = nameof(Switch))]
public partial class Switch
{
}

