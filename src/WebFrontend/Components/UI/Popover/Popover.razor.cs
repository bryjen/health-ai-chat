using Microsoft.AspNetCore.Components;
using WebFrontend.Utils;

namespace WebFrontend.Components.UI.Popover;

[ComponentMetadata(
    Description = "Popover root providing open/close state to trigger and content.",
    IsEntry = true,
    Group = nameof(Popover))]
public partial class Popover
{
}

