using Microsoft.AspNetCore.Components;
using WebFrontend.Utils;

namespace WebFrontend.Components.UI.Progress;

[ComponentMetadata(
    Description = "Progress bar component for visualizing completion percentage.",
    IsEntry = true,
    Group = nameof(Progress))]
public partial class Progress
{
}

