using Microsoft.AspNetCore.Components;
using WebFrontend.Utils;

namespace WebFrontend.Components.UI.Badge;

[ComponentMetadata(
    Description = "Small badge for status, counts, or labels.",
    IsEntry = true,
    Group = nameof(Badge))]
public partial class Badge
{
}

