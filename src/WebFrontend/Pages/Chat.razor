@page "/chat"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@using WebFrontend.Components.Core.Chat

<PageTitle>Chat</PageTitle>

<div class="min-h-screen h-screen bg-background">
    <div class="sticky top-0 z-10 border-b border-border relative">
        <div class="absolute inset-0">
            <div class="h-16 w-full bg-gradient-to-b from-background/90 via-background/70 to-transparent"></div>
            <div class="absolute inset-0 bg-gradient-to-b from-background/60 via-background/40 to-transparent backdrop-blur-[24px]"></div>
        </div>
        <div class="absolute inset-x-0 top-0 z-20 flex h-16 items-center justify-between px-4">
            <div class="text-sm font-medium tracking-wide text-foreground/80 uppercase">Live health assistant</div>
            <div class="text-xs text-muted-foreground">Connected · @(IsConnected ? "Yes" : "Reconnecting...")</div>
        </div>
    </div>

    <div class="flex flex-col h-[100vh] justify-between">
        @if (Messages.Count == 0)
        {
            <div class="flex-1 flex items-center justify-center">
                <div class="text-center">
                    <div class="mb-6 flex flex-col items-center justify-center gap-2">
                        <svg class="-mb-2 w-10 h-10 text-primary" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                        </svg>
                        <h1 class="!text-4xl font-semibold text-foreground">
                            @GetGreeting()
                        </h1>
                        <span class="text-sm text-foreground">
                            How may I help you today?
                        </span>
                    </div>
                </div>
            </div>
        }
        else
        {
            @* Active Chat View *@
            <ChatMessageList Messages="Messages" IsLoading="IsLoading"/>
        }

        <div class="relative flex flex-col">
            <div class="absolute z-10 bottom-full left-0 right-0 h-8 bg-gradient-to-t from-background via-background/80 to-transparent pointer-events-none"></div>

            <ChatInput
                InputText="@InputText"
                InputTextChanged="OnInputTextChanged"
                IsLoading="@IsLoading"
                IsConnected="@IsConnected"
                OnSubmit="@HandleSubmit" />
        </div>


    </div>
</div>
